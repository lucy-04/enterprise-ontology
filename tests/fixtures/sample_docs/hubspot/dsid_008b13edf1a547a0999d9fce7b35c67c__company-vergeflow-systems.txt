VergeFlow Systems

Summary: VergeFlow runs a real-time personalization engine for programmatic ads (user segmentation + reranking) — extremely latency- and cost-sensitive. Wanted Redwood for: predictable p95 during bid windows, cheaper per-token for high QPS reranking, and easy rollback to smaller models.

Recent call highlights: 
- CTO (A. Mendes): "If p95 jumps above 80ms during peak we lose auctions — need hard guarantees and fallback plan."
- Wants quantization recommendations (8-bit, 4-bit) for Llama/Mistral variants and a regression testing plan.
- Interested in caching prefix KV across sessions to reduce repeated context cost; asks about TTLs and eviction policies.

Technical requirements / asks: 
- Target SLOs: p50 < 15ms, p95 < 80ms end-to-end for rerank requests (avg seq len 64 tokens).
- Throughput target: sustained 500-700 qps on rerank endpoint during peak minutes, burst to 1.2k qps.
- Cost sensitivity: target < $0.08 per 1k tokens equivalent for rerank flows (high volume).
- Model stack preference: quantized Llama2-70B/13B variants and Mistral-small for canary. Must have automatic fallback to smaller quantized variant when latency spikes.
- Observability: token-level cost breakdown, per-route latency heatmap, rollback/canary hooks.
- Security: SSO/SAML + audit logs mandatory, need KMS integration for Dedicated.

Notes on Redwood fit and questions: 
- Keen on Redwood Dedicated for reserved capacity + controlled autoscaling.
- Wants Redwood Optimize to propose batching config (max batch latency budget), caching policies and quantization profile that balance regression risk vs cost.
- Will request a perf benchmarking suite run (we supplied sample harness) and compare to their current Triton-based infra.

Internal AE notes: 
- Good technical engagement from infra lead, they can provision trial GPUs in their AWS account but want Redwood-managed Dedicated test first.
- Legal prepping a short addendum for EU data residency if POC looks good.
- SE action: build benchmark matrix (see linked_drive_docs) and include model variants: llm-13b-q8, llm-13b-q4, mistral-7b-q8, and a fallback 3B quantized model.

Stubs/links: Fireflies transcripts attached, pricing deck shared, want formal quote after POC.
Run 2-week Dedicated perf POC (benchmark harness + production-like traffic) — SE to schedule infra and test matrix
legal review on data residency clause
need hard latency p95 proof vs current Triton cluster
quantization/regression risk concerns
2026-01-20: Intro call (AE Jordan + CTO) — high level requirements
2026-01-27: Technical deep dive with SE — discussed batching/caching/quantization options
2026-02-15: Demo + initial perf conversation (fireflies ff_20260215_0845_vergeflow)
2026-02-28: Shared benchmark harness + sample traffic (drive link)
2026-03-03: Security questionnaire submitted (SEC-88 opened)
2026-03-09: Redwood to propose Dedicated POC topology and pricing; SE to schedule run matrix