airgap-minimal-shrinkwrap-and-almalinux-support-with-registry-replication-module

Motivation: support customers that must run private/on-prem installs on RHEL derivatives after CentOS8 EOL and enable much smaller, curated airgap bundles that operators can generate for minimum-viable installs. This PR adds AlmaLinux 8/9 node support to the installer images and packer build pipelines, introduces a 'shrinkwrap' bundle manifest and generator that produces component-level minimal bundles for air-gapped installs, and provides a Terraform module to replicate selected container images into a local registry (with optional Content Trust and signed manifests). The Helm chart changes add an initPreload hook template to load images into local registries before startup and a TLS-pinning option for registries with self-signed certs. Operational improvements: preflight checks for kernel modules and container runtimes were extended for AlmaLinux variants, and we add an atomic rollback script + helm rollback wrapper that replays saved release manifests from delta bundle metadata. Backwards compatibility: old full-bundle installer remains the default; shrinkwrap is opt-in via `bundle-profile=shrinkwrap` and has a documented schema. Includes docs and examples for generating a shrinkwrap bundle, configuring registry replication, and an e2e smoke test for an AlmaLinux VM in an air-gapped network. Checklist: [x] packer image builds for AlmaLinux8/9, [x] shrinkwrap spec + generator, [x] terraform registry-replication module, [x] helm initPreload and cert handling, [x] preflight & rollback scripts, [x] docs and usage examples, [x] CI e2e on GCP VM approximating airgap, [x] manual verification on customer-sim cluster. Related: ENG-8132 for shrinkwrap design doc, SUP-2211 customer report on CentOS8 deprecation. Breaking change: none by default; shrinkwrap opt-in.
8 commits: add packer/almalinux builds and CI job; implement shrinkwrap manifest schema and generator tool; terraform module for registry replication with examples; helm chart: initPreload template + registry TLS handling; add preflight checks and atomic-rollback script; add docs + examples; fix linting and add e2e test; bump chart apiVersion for compatibility.
Rachel Kim (2024-11-16): Left a couple of nits on the shrinkwrap schema (optional fields and default semantics). Please document how to recover when a component is omitted in a shrinkwrap bundle.
Sofia Martinez (2024-11-16): Responded with details and added a 'recovery.md' note to the docs; adjusted schema defaults to preserve optional components and added explicit error messages in generator.
Devon Patel (2024-11-17): Question on the terraform module: can we support OCI registry replication in addition to Docker v2? Marked tfsec WARN for an IAM policy that looks broad; requested scoped policy example.
Sofia Martinez (2024-11-17): Added scoped example and tightened IAM docs; left a follow-up to add an automated policy generator in a follow-up PR (ENG-8201).
Ibrahim Bashir (2024-11-17): Verified helm initPreload handles tarball and OCI layout registries; suggested a small change to retry logic which was applied.
CI Bot (2024-11-18): All checks passed after final lint fixes; integration e2e completed successfully.
Adds AlmaLinux 8/9 packer builds for air-gapped installs; introduces 'shrinkwrap' bundle profile for generating minimal component bundles; new Terraform module for replicating selected images into a local registry; Helm chart changes to preload images and handle self-signed registry certs; atomic rollback tooling for delta upgrades. Opt-in features—no default breaking changes.