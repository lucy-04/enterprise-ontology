KMS chaos failover harness and compact evidence bundler

Build a repeatable chaos-testing harness that simulates realistic KMS/HSM failover scenarios across regions and tenant configurations, and implement a Compact Evidence Bundler (CEB) that produces signed, compressed rotation and access evidence artifacts for auditors. Goal: prove that key rotation and wrap/unwrap workflows remain correct under transient KMS errors, region failovers, and during dual-KMS migration, while minimizing the storage footprint and ingestion time of audit evidence. Deliverables: (1) chaos harness with scenario definitions and automation, (2) evidence bundler service that aggregates rotation logs, wraps them with a deterministic envelope, compresses, and signs with a non-customer root for submission to the compliance pipeline, (3) observable dashboards and health checks, (4) test matrix documenting coverage per-customer and per-deployment-mode (hosted / dedicated / private). This ticket focuses on the first implementation slice (harness + basic bundler output format).
Chaos harness can run 10 predefined scenarios (regional KMS outage, HSM latency spikes, KEK provider failover, truncated responses) against a staging fleet and report pass/fail per-tenant
Compact Evidence Bundler emits a signed tar.gz per rotation window that contains a manifest.json, rotated key metadata, rotation proofs (wrap/unwrap events), and the minimal tokenized audit pointer format
Produced evidence artifacts are no larger than 20% of equivalent raw logs when run against the sample 10k-req dataset
End-to-end test shows that a consumer (audit-ingest pipeline) can verify signature and parse the compact bundle within 5s
No unhandled leaks: chaos harness must not leave ephemeral keys in production; all simulated KEKs are scoped to the test namespace and cleaned up
Security review completed and automated signing key stored in HSM with rotation policy; doc updated with threat model notes
Define failure scenarios and runbook for each (owner: Maya)
Prototype chaos harness using localinject + fleet runner (owner: Arun)
Design Compact Evidence Bundler file format and manifest schema (owner: Maya)
Implement bundler service with streaming gzip and HSM signing integration (owner: Priya)
Add CI job to run harness nightly against staging and snapshot artifacts to S3/staging-audit (owner: CI team)
Create dashboard panels (per-scenario pass rate, artifact size, ingestion latency) (owner: Observability)
Run tabletop with Compliance and Security to validate retention and upload guarantees (owner: Diego)
Prepare PRD appendix for enterprise customers describing how to request expanded evidence (owner: PM)
Write cleanup and safety checks to ensure ephemeral keys removed after each run (owner: Arun)
Prepare migration notes for Dedicated/Private deployments where customer-managed KMS is used (owner: Maya)
2026-02-19 - Diego: Kickoff meeting notes attached to PRD; Compliance asked for an artifact size target — we set 20% target vs raw logs.
2026-02-25 - Maya: Architecture review completed with Security. Decision: use an internal non-customer signing key stored in HSM; bundler signs the manifest and a meatadata tombstone for verifiability.
2026-02-28 - Arun: Initial harness prototype can inject latency and 503s against the KMS shim. Found race where concurrent rotation and failover caused duplicate wrap events — added idempotency token to rotation controller.
2026-03-03 - Priya: Proof-of-concept bundler outputs gzipped tar with manifest.json and signature file; size reduction ~27% on 10k sample (needs verification across larger dataset).
2026-03-06 - Observability: Dashboard panels staged in staging Grafana; ingestion latency panel shows 8s median for unpack+verify — above 5s goal but prototype uses non-optimized parser.
2026-03-09 - Security: Requested explicit threat model update covering canonicalized manifests and replay protections; blocked signing rollout until HSM access policy patch is merged (security#452).
2026-03-10 - Maya: Merged idempotency and cleanup fixes to harness; next: integrate HSM signing API and add nightly CI run.